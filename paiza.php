<?php
    
    // 【入力された値を比べて最大値を算出】
    // $count = fgets(STDIN);
    // $max = 0;

    // for ($i = 0; $i < $count; $i++) {
    //     $number = (int)fgets(STDIN);
    //     if ($number > $max) {
    //       $max = $number;
    //     }
    // }

    // echo $max;
    // ------------------------------------

    $count = fgets(STDIN);
    for ($i = 0; $i < $count; $i++) {
      echo "paiza ";
    }

?>